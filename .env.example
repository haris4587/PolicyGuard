# Optional build-time override. The verified deployment record in
# config/deployment.json is used when this remains blank.
NEXT_PUBLIC_POLICYGUARD_CONTRACT_ADDRESS=
NEXT_PUBLIC_GENLAYER_RPC_URL=https://studio.genlayer.com/api
